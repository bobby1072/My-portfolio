import { createTheme } from "@mui/material";
export const mainTheme = createTheme({
  palette: {
    primary: {
      main: "#C12929",
    },
    secondary: {
      main: "#000000",
    },
    success: {
      main: "#AAFF00",
    },
  },
});
