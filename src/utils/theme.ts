import { createTheme } from "@mui/material";
export const mainTheme = createTheme({
  palette: {
    primary: {
      main: "#483285",
    },
    secondary: {
      main: "#000000",
    },
    success: {
      main: "#AAFF00",
    },
  },
});
